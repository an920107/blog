import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from '$lib/common/framework/components/ui/button/button.svelte';

export {
	//
	Root as Button,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
	type ButtonProps as Props,
	Root,
};
